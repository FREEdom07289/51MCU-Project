#include <REGX52.H> // 包含 8052 单片机的寄存器和位定义，方便使用端口和位操作
sbit LED1 = P2^0; // 定义位变量 LED1 映射到 P2 端口的第0位（P2.0）
void main() // 主函数入口：程序从这里开始执行
{ // main 函数开始
    LED1 = 0; // 将 P2.0 置 0，通常表示点亮 LED（视硬件接法而定）
    while(1) // 无限循环，保持程序持续运行
    { // while 循环开始（当前为空，可放置其他逻辑或延时）
        // 空循环：占位，不断重复，可在此添加延时或外设处理
    } // while 循环结束

} // main 函数结束
